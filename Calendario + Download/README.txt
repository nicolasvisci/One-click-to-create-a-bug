Ho inserito i file per il calendario e per il download del questionario di anamnesi.

Basta inserire i file nei controller, model e view e dovrebbe funzionare.
ovviamente i file già esistenti vanno solo aggiornati con le nuove funzioni che ho inserito.

Per il calendario inserite il file script.js nella cartella public (in fondo alle cartelle di codeigniter)

Eseguite la query su phpmyadmin per creare la tabella events e nel momento in cui inserirete un nuovo evento nel database vi comparirà nel calendario.